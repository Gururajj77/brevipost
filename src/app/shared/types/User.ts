export type User = {
    email: string;
    name: string;
    uid: string;
    photoUrl: string;
    isFollowing: string;
    followerCount: string;
    followingCount: string;
    postsCount: string;
}
